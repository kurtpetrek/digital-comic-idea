import React, { Component } from 'react';
import styled from 'styled-components';

export default class Tile2Container extends Component {
  costructor(props) {
    super(props);
    this.state = {
      animationPlayState: 'paused'
    }
  }

  render() {
    return (
      null
    )
  }
}
